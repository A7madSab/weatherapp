import { createTheme } from "@mui/material/styles"

const lightTheme = createTheme({
    typography: {
        fontFamily: "Roboto",
    },
    palette: {
        mode: "light",
    },
})

export default lightTheme
